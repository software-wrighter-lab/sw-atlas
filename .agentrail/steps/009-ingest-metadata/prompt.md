Read docs/plan.md section 8 first. Write 'atlas-ingest repos' and 'atlas-ingest videos'. Repositories: id, organization, name, description, topics, language, homepage and last-update from the GitHub API, through a committed cache so a rebuild needs no network and the corpus is reproducible. Videos: title, url, date and associated project, from the blog's own video_url and video_title fields plus a committed supplement for anything they miss. Metadata only for now, but not symmetrically. Repository source ingestion is deferred on principle: the claim stays 'I know about this repository', not 'I know what is in it'. Video content is deferred only on availability -- the scripts exist as authored text in git repositories that are not cloned on this machine yet (plan.md section 12.3), so they are prose someone wrote rather than transcripts to be cleaned up. Design the video resource so that when those repositories arrive, an ingester fills in a script field the way ingest-blog fills in an abstract, with no widening of the type and no migration. A video gets the same abstract, concepts and aliases a post gets; today the script is empty rather than absent. Exit: both sources in the corpus with their counts, the cache committed and documented, just check green.