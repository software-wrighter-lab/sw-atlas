Read docs/hybrid-docent.md section 6 first. Write 'just eval': intent accuracy, destination accuracy, top-3, MRR, unsupported recall, ambiguous top-2, ECE, Brier, p50/p95 latency and bytes, over every frozen set, for every arm. Add MB02-oracle@k, the share of questions whose expected answer is in the matcher's top k for k in 1..10 -- the ceiling for anything that reranks. Paired statistics because every arm answers the same questions: McNemar on per-question correctness and a paired bootstrap 95 percent interval on every margin, reported beside the margin and never without it. The harness reports the regression rows by text, not merely counted.