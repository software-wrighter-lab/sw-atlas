Read docs/plan.md section 10, Saga 1 step 5 first. Write 'just report': regenerate docs/reference/coverage.md from the corpus with artifacts indexed per kind, resources tagged, resources related, orphans and broken URLs. Two gates make the command exit non-zero: any orphan resource (one unreachable through concepts and relations) and any broken URL. Check every Resource url over the network with a committed cache and a clearly marked offline mode, so the gate is honest but a rebuild is not hostage to someone else's outage. The document is written by the run, never by hand, and the README quotes its numbers. This is the step that turns 'Atlas knows about everything' from an aspiration into a number with a gate behind it. Exit: the report generated, both gates demonstrated failing on a seeded fault and passing on the real corpus, just check green.