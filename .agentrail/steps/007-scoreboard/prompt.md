The harness writes the measured MB02 and MB02-oracle@k rows into docs/reference/results.md rather than a person writing them, the README quotes them, and the saga states plainly what the oracle number means for Saga 3: whether a reranker has room to win at all, or whether a second candidate source (A1 embedding retrieval) has to come first. If the oracle@5 is near dest@1, say so as the finding and re-plan Saga 3 around it.