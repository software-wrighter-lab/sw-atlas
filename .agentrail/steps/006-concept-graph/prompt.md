Read docs/plan.md section 7 first. Build the Concept set from the union of blog tags and keywords, campus concepts, and repository topics: normalize them into Concept records with aliases and parents, so that 'moe', 'mixture-of-experts' and 'mixture of experts' are one concept and 'apl' the language is not merged with an unrelated acronym. Emit an alias collision report naming every merge the normalizer made and every near-miss it declined, because that report is the only way a human can audit it. Add a committed override file for the cases it gets wrong, applied deterministically after normalization, so corrections survive a rebuild. This is the step where a reviewer should expect to spend time reading output rather than code. Exit: the concept graph, the collision report as a generated document, the override file with its first real entries, and just check green.