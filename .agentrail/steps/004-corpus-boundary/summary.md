Video content is deferred on availability, not principle: the scripts are authored text in git repositories not yet cloned here, so ResourceKind::Video is designed with an empty script field rather than as a metadata-only shape needing later widening; ingest-metadata's prompt says so. Repository source stays deferred on principle. Plan now carries the higher figure where a count is uncertain (roughly 130 repositories), because an under-counting corpus gates green while missing things. blog-requests.md's future-asks entry rewritten since it framed video knowledge as a transcription problem. just check green.