Two facts from the repository owner change the corpus boundary and must be recorded before the schema is written, because the schema has to leave room for them. First: the higher repository count is the right one -- roughly 130 public repositories, not the research note's 86 -- and where an estimate is uncertain the plan should carry the higher figure, since a corpus that under-counts itself produces a coverage gate that passes while missing things. Second, and more important: the video scripts already exist as authored text in git repositories that are not cloned on this machine yet. That changes open decision 3 in docs/plan.md section 12 substantially. The deferral of video content was justified by the cost and inaccuracy of transcription; if the scripts are written text under version control, there is nothing to transcribe and the objection disappears. Record this as a source that is deferred only until the repositories are available, not deferred on principle, and note what it would take: the repository names, a clone, and an ingester that reads authored scripts rather than machine transcripts. Update docs/blog-requests.md's future-asks section accordingly, since it currently frames video knowledge as a transcription problem. Update the ingest-metadata step prompt so a later session knows scripts are expected to arrive and the video resource type should not be designed as metadata-only forever. Exit: both facts recorded where the schema and ingester sessions will read them, just check green.