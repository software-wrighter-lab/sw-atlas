Fixed two link-to-resource faults found by reading the corpus, neither visible to the coverage gates. (1) link_id trimmed a trailing slash but not a trailing fragment, so the campus front page was two demo resources; the tail is now normalized (fragment, then slashes, after whitespace) and tests pin the real duplicate pair plus the client-side-route case. (2) Demos titled 'run' and 'io demo' took a button label as a name; a short committed label list is now treated as no title, for Demo and Video kinds only (a citation's name stays the declarer's business). Result: 642 resources not 643, zero duplicate ids, no title that is a verb, 641 URLs. Coverage regenerated; counts corrected in README, results.md and the closed-saga record in sagas.md (kept, with the correction noted). Work orders filed: DEMO-NAMES in sw-campus-requests.md and a paragraph in blog-requests.md, since those repositories are the only places that know what the demos are called. sw-checklist 94/0/0, 71 tests.