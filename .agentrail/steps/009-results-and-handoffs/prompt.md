Read docs/plan.md sections 10 and 11 first. Close the saga. Seed docs/reference/results.md with the four transferred moe-microscope rows (MB01 0.630 paraphrase destination, MB01t 0.685, CD01 0.296, CD01b 0.407, each with its intent accuracy, unsupported recall and stored bytes) keeping their origin beside them, and the empty columns Saga 2 will fill. Write docs/handoffs.md with the three moe-microscope work orders from plan.md section 11 (the reframed tiered-weight experiment, the request to check the no-FFN claim on the docent corpus it already has, and the MoE variant held as the fallback), plus anything the ingesters found that sw-campus or blog should change: this repository cannot edit them, so a work order and a line in the session summary is the whole mechanism. Update the README status section with the real coverage numbers. Queue Saga 2 in docs/sagas.md. Exit: the saga closed with agentrail complete --done, and a session summary naming what the next saga needs from whom.