Read docs/plan.md section 8 first. Write 'atlas-ingest repos' and 'atlas-ingest videos'. Repositories: id, organization, name, description, topics, language, homepage and last-update from the GitHub API, through a committed cache so a rebuild needs no network and the corpus is reproducible. Videos: title, url, date and associated project, from the blog's own video_url and video_title fields plus a committed supplement for anything they miss. Metadata only. No source files, no transcripts, no frame analysis: the claim this repository makes is 'I know about this repository', not 'I know what is in it', and that boundary is a non-goal in the plan, not an oversight. Exit: both sources in the corpus with their counts, the cache committed and documented, just check green.