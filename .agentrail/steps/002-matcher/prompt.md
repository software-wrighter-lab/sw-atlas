Read docs/plan.md section 10 (Saga 2) first. Port moe-microscope's MB01 and MB01t matcher from 9 campus places to all 643 artifacts: alias, concept, title and abstract signals with the MB01 weights (title 2, aliases 3, concepts 4, tagline 1.5, partial 0.8, phrase +4), the MB01t variant over all catalog text, plus graph traversal over relations. Ported, not reinvented. Any deviation from those weights is recorded as a deviation with its reason, because the MB02 row is comparable to MB01 only if the weights are the same weights. The matcher is A0: no model, inside 10 MiB, under 10 ms, and it is a permanent runtime class that is never deleted.