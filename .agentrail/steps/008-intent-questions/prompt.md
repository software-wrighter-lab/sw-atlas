AT01 showed a five-class intent head scoring 0.697 against a 0.737 always-FindResource baseline, because 72% of the 386 confirmed questions are FindResource: they were drafted to test whether the right resource is found, and a set drafted to test destinations cannot teach or measure intent. Draft an intent-balanced supplement, sources/questions/intent.ron, spread across Navigate, Explain, Status, Recommend, Compare, Story and a few Unsupported, in numbers that make a majority-class baseline weak rather than strong. Rows are origin Teacher and status Unconfirmed: the owner confirms, as he did the others. The no-alias rule does not apply here -- an intent question may name its subject, because that is how a visitor asks about status or comparison -- so the set's note must say what the rows are for and how they are scored: intent always, destination only where a row names one. Where a destination is obvious, give it, so a row measures both; where the question is genuinely open, leave expect empty and say so. Regenerate sources/questions/REVIEW.txt so the owner has one file to edit, and say in the set note that Compare is the intent A0's keyword rules cannot express at all. Exit: the set committed, just questions green with the new counts, just check green.