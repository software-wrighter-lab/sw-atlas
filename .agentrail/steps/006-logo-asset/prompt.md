The logo in images/ has a typo in the artwork. The corrected 1024x1024 JPEG is at ~/Downloads/sw-atlas-logo.jpg. Replace the repository's copy with it, and produce a web asset whose corners are transparent so the square artwork does not sit as a hard-edged box against the README and the page background, light or dark. JPEG cannot carry an alpha channel, so the web copy is PNG. Use ImageMagick or sips, never a Python script (sw-campus's asset rule, and this repository's no-Python rule). Keep the corrected JPEG as the source of record in images/ and put the transparent copy beside it. Sample the actual corner pixel colour rather than assuming white, and choose a corner radius by looking at the artwork rather than guessing. Update README.md to reference the transparent copy. Exit: both files committed, the README rendering against the new asset, and just check green.