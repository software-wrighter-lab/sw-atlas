Read docs/plan.md section 8 first. Write 'atlas-ingest campus ../sw-campus': the nine places in pages/docent/snapshot-a.json become Resources with PartOf relations from its parents array, and its kinds, titles, taglines, status, links, aliases and concepts map onto the schema. The 21 stories are catalog text: they are answer material, never training text, and the distinction must be visible in the types. sw-campus plans to publish dist/catalog.json canonically with its SHA-256; read that when it exists and snapshot-a.json until then, recording which was used. The ibm-1442 card reader and its radio demo are excluded from snapshot A on purpose and Saga 6 depends on that exclusion: assert it with a test that fails if a 1442 resource ever appears from this source. ../sw-campus is read-only; anything it needs to change goes in docs/sw-campus-requests.md. Exit: nine resources, the exclusion test, just check green.