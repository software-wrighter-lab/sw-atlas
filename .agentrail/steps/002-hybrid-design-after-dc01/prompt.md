demo-decision-model delivered both sw-atlas asks on 2026-09-23/24, and one of them came with a measurement that changes this project's design. PR05 exists (lib/scorer.mlpl, crates/tdm-model/src/scorer.rs, Scorer::rank taking candidates as text, 1e-9 parity, pinnable at tdm-v0.1.0) and their lesson DC01 reports: where candidate cards trained, the question-conditioned scorer costs about one point against a fixed head (85.3% against 86.1%); where cards were held out of training entirely it scores 0.4% in a 51-card field, 36.5% in a five-card field, and chance (18.6%) when every candidate is equally cold. Their caveat is fair -- a DOCTOR rule card shares only function words with its input, while an Atlas query and card share content words -- so it is a lower bound from an unfriendly regime and needs its own measurement here. What it kills is the specific claim hybrid-docent.md rests on: that a post published after training can be reranked into first place. Update the design honestly. Make concepts the bridge: the model emits intent, resource kind, a multi-label concept head over the ~703 curated concepts, and the Nouls -- all fixed heads, so none of them can be cold -- and deterministic code resolves concepts to resources, which is how a newly indexed post becomes reachable with no retraining and no cold-card scoring. Confine reranking to a small field of cards the model trained on; cold candidates keep the matcher's order, which also makes 'never worse than A0 on a new post' structural rather than hoped for. Adopt their hold-out harness shape as a planned measurement (full field, small warm field, small cold field; the cold field separates weak from biased) and record the DC01 numbers with their origin beside them. Also record LB01: a local 3B model zero-shot beat their trained model 0.792 to 0.750 on spam and phishing by the same bounded-choice method and was better calibrated untouched than theirs after temperature scaling, so state plainly what this project's claim is (offline, cheap, cannot invent an exhibit, knows a private corpus no pretrained model has seen) and what it is not (more accurate than a large model with the catalog in its prompt). Update docs/demo-decision-model-requests.md from waiting to delivered for both asks, keeping the caveat we must measure ourselves, and note that they now carry docs/reference/downstream-requests.md so the relay is no longer the only channel. Touch plan.md section 10 Saga 3, docs/sagas.md and the README where they describe the hybrid. Documents only. Exit: just check green, every relative link resolves, no number without its origin.