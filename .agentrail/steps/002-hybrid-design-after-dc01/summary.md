Recorded upstream's delivery of both asks and redesigned around the measurement PR05 arrived with. DC01: a card scorer gets 85.3% where cards trained, 0.4% full field / 36.5% five-card / 18.6% all-cold where they did not, so the claim that a post published after training can be reranked into first place is gone. Redesign: every head a fixed label set (intent, kind, multi-label concepts over the curated vocabulary, Nouls); concepts plus the catalog carry a newly indexed resource; reranking confined to warm cards with cold ones keeping the matcher's order, making 'never worse than A0 on a new post' structural; upstream's hold-out harness (full/warm-k/cold-k) adopted for our own measurement, since their regime is harsher than ours. Also recorded LB01 (local 3B beat their trained model zero-shot 0.792 vs 0.750, better calibrated) and stated what this project claims and does not, plus their in-browser training result. Updated hybrid-docent.md sections 2/2a/2b/3/5/6/7, plan.md Saga 3, docs/sagas.md, README and demo-decision-model-requests.md (both asks delivered; they now carry downstream-requests.md).