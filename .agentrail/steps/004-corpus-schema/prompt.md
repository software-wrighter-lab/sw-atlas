Read docs/plan.md section 7 first. Define the contract every later step depends on, in the atlas-core crate: Resource, Concept, Relation, Question and Decision, with ResourceId and ConceptId newtypes, and the ResourceKind, RelationKind, Provenance, Maturity, Split and Origin enumerations. RON serde both directions. A canonical writer that emits a byte-stable form and a content hash over it, so a rebuild that changes nothing produces an identical file. Cross-reference validation as a native test: every relation endpoint resolves to a Resource, every concept reference resolves to a Concept, and a deliberately broken fixture fails it. Provenance is load-bearing (Declared from an author's front matter, Derived from deterministic analysis, Teacher from a model) and every metric must be recomputable with Teacher rows excluded, so design for that now. Exit: the types, the writer, the hash, the validator, tests over a small committed fixture, and just check green.