Read docs/plan.md section 10 (Saga 2) first. Port moe-microscope's MB01 and MB01t matcher from 9 campus places to all 642 artifacts: alias, concept, title and abstract signals with the MB01 weights (title 2, aliases 3, concepts 4, tagline 1.5, partial 0.8 for a substring of five characters or more, phrase +4), the MB01t variant over all catalog text at weight 1, plus graph traversal over relations. Ported, not reinvented: any deviation from those weights is recorded as a deviation with its reason, because the MB02 row is comparable to MB01 only if the weights are the same weights. Emit the top k with the signals that fired, because the eval harness needs candidates and 'why this answer' needs the signals. Intent comes from the campus mockup's keyword rules, ported as data rather than as regexes buried in code. The matcher is A0: no model, inside 10 MiB, under 10 ms, a permanent runtime class that is never deleted. This step supersedes the identically-named step 003, which an earlier reorder left behind the cursor; 003 is not worked and its prompt is the same text.