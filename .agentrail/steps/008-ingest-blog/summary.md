just ingest reads ../blog and writes a canonical corpus from front matter alone: 124 posts, 76 repos, 85 videos, 6 demos, 153 papers, 617 provisional concepts, 508 relations, all Provenance::Declared. Rebuild produces identical bytes and hash. Two real shapes accommodated after running it rather than assuming: video_urls as bare strings with a parallel video_titles list, and a post naming the same repo in both repo_url and repo_urls (one resource, one edge). Ids derive from URLs so two posts naming one repo produce one resource. Nine tests pin it against the front matter of three real posts. Parse failures name the post and field and stop the run; no skip-and-continue. atlas-blog reads, atlas-ingest assembles as lib+bin so assembly is testable; Resource::stub moved to atlas-core. CLI meets the house --help/--version contract via build.rs and is installed with sw-install. just check green: 19 tests, 37 checks, 0 failed, 0 warnings.