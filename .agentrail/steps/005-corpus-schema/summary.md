Schema landed as three crates (sw-checklist allows four modules per crate; the split is meaningful): atlas-core with ids, catalog (Resource, ResourceKind, Maturity, Concept) and relations; atlas-decide with Decision, Intent, Question, Origin, Split; atlas-corpus with the collection, the cross-reference validator and the canonical writer. Resource.body holds authored long-form text, empty rather than absent, so video scripts arrive without widening the type -- asserted by a test. Decision carries no prose, URL or resource id: invariant 1 as a type. Canonical writer sorts everything before serialising so shuffled input yields identical bytes and hash; one edited word changes it. Validator returns every problem sorted with the context it was found in; broken fixture exercises all three kinds. just check green: 10 tests, sw-checklist 16 passed 0 failed 0 warnings.