Read docs/plan.md section 8 first. Write 'atlas-ingest repos' and 'atlas-ingest videos'. Repositories: id, organization, name, description, topics, language, homepage and last-update from the GitHub API, through a committed cache so a rebuild needs no network and the corpus is reproducible. Videos: title, url and date from the blog's own video_url and video_title fields, which are the only complete record of what is published and where. The spoken text comes from ~/github/softwarewrighter/shorts (see docs/video-sources.md): episode N of Five ML Concepts maps to projects/5MLC-N deterministically, except episode 1 which is projects/daily5-20260203, and that rule may be code. Every other video needs a committed join map of video_url to shorts project, written by hand and carrying Provenance::Declared, because titles changed between production and publication -- projects/trm's description opens 'Under 1000 Parameters Beats GPT-4 at Mazes' while its published title is '976 parameters is more than billions?!'. Do not match titles at ingest time; a wrong relation that looks right is the failure this project exists to avoid. The concept-to-episode edges in shorts/docs/concepts-status.txt (130 concepts, 26 episodes) are Declared, not derived. Metadata only for now, but not symmetrically. Repository source ingestion is deferred on principle: the claim stays 'I know about this repository', not 'I know what is in it'. Video content is deferred only on availability -- the scripts exist as authored text in git repositories that are not cloned on this machine yet (plan.md section 12.3), so they are prose someone wrote rather than transcripts to be cleaned up. Design the video resource so that when those repositories arrive, an ingester fills in a script field the way ingest-blog fills in an abstract, with no widening of the type and no migration. A video gets the same abstract, concepts and aliases a post gets; today the script is empty rather than absent. Exit: both sources in the corpus with their counts, the cache committed and documented, just check green.