Read docs/hybrid-docent.md section 6 and section 2a-bis first. Write 'just eval': for every arm, over every CONFIRMED frozen set, report intent accuracy beside its majority-class baseline (AT01 scored 0.697 where always answering FindResource scores 0.737, and a bare accuracy would have read as a result), destination accuracy at 1, recall at k for k in 1..20, MRR, unsupported recall, ambiguous top-2, p50 and p95 latency, and the signal count as stored bytes. recall@k is the headline: it is the ceiling on anything that reranks, their stand-in matcher capped at 0.63 with k=20, and MB02's own number decides whether Saga 3's reranker has room to win at all. Arms: MB02 (fields), MB02t (text), and MB02g (one declared hop, the separate graph arm), so the graph's value is a measured margin rather than a claim. Paired statistics because every arm answers the same questions: McNemar on per-question correctness between arms and a paired bootstrap 95 percent interval on every margin, reported beside the margin and never without it. List the regression rows by text, not merely counted. The harness writes a table a person can read and a machine-readable row for the scoreboard step; it does not write docs/reference/results.md itself. This step supersedes step 004, which the cursor stranded.