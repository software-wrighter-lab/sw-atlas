All 386 rows confirmed by the repository owner on 2026-09-24: 308 paraphrases by section (17 x 'Y ALL'), exact-name 16, ambiguous 12, off-topic 20, follow-up 10 row by row, and the meta set marked 'Meta' on every row -- recorded as confirmed with the set note now stating that meta answers are computed from the catalog or replayed from the trace, not retrieved, and scored on whether the policy routed to a meta answer. Nothing rejected; set hashes unchanged by confirmation. Committed sources/questions/REVIEW.txt as the record of the pass, plus scripts/review-form and an atlas-questions --form flag so the next round is one command. Found and fixed a bug in my own form: '|' as the column separator misread ten follow-up rows (whose questions contain '|') as owner edits; separator is now ' :: ', and the form-vs-source comparison that caught it is why nothing wrong reached the corpus. Leakage check and the seeded-fault tests from the earlier batch still pass. Gate: 71 tests, sw-checklist 94/0/0.