[package]
name = "atlas-signals"
description = "The signal index behind MB02: every word of every resource, in the bucket that says how much it counts for"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
atlas-core.workspace = true
atlas-corpus.workspace = true
