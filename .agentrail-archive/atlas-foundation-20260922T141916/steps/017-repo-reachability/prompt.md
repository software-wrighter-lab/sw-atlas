Found while starting coverage-report: 15 repositories are orphans -- no concept and no relation -- so the zero-orphan gate cannot pass honestly and a visitor could never be sent to them. They have a description but no topics, no language, and no post links to them. Two fixes, both committed and both documented with their reasoning. (1) Every repository gains its GitHub organisation as a concept, so 'what is in sw-comp-history' and 'what have you published under hardwarewrighter' are answerable and no repository is unreachable. State plainly in the crate docs that an organisation is a coarse subject grouping rather than an idea, and record the alternative that was rejected: a ResourceKind::Organisation, which would widen the schema and add a kind the model must predict mid-saga. (2) A committed exclusion list, sources/repo-exclusions.ron, for repositories that are not publishable artifacts at all -- softwarewrighter/images is static images for GitHub Pages and softwarewrighter/placeholder is 'repo for future use'. Each row carries the reason it is excluded; the ingester drops them and the coverage report states how many were excluded and why, so the exclusion is visible rather than a silent shrinking of the denominator. Do not exclude anything merely because it is thin: a real project with a description belongs in the corpus. Update the pinned counts in crates/atlas-repos/tests, the README, cache/README.md and docs/sagas.md. Exit: zero orphans among repositories, the exclusion list with its first entries and reasons, just check green.