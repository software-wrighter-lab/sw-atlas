Two owner decisions arrived mid-saga and both change documents rather than code. First: no Python, Rust and/or sw-MLPL only. That settles open decision 1 in docs/plan.md section 12 against option B (finetune upstream Needle offline in JAX) and against any one-off Python measurement. Rewrite that decision as resolved, with the date and the consequence: Needle's architecture is adopted but its toolchain is not, so the Saga 3 probe has to be a Rust forward pass over the open weights rather than a JAX run, and docs/needle.md section 5 must stop recommending B. Say plainly what the decision costs -- the cheap measurement is no longer free -- because a resolved decision that hides its own price is worse than an open one. Second: asks on other repositories live in docs/<repo>-requests.md, one file per repository, not in a single handoffs.md. Create docs/moe-microscope-requests.md with the three work orders from plan.md section 11, docs/sw-campus-requests.md with the dist/catalog.json canonical export and the 1442 snapshot-B event, and docs/blog-requests.md recording that the front matter is already the corpus and what would break if it changed. Update every reference to docs/handoffs.md in CLAUDE.md, docs/plan.md, docs/sagas.md and step 008's prompt. Exit: both decisions recorded where a future session will actually read them, the three request files real rather than placeholders, and just check green.