Read docs/plan.md and CLAUDE.md first. Stand up the Rust workspace so every later step has a gate to pass. Cargo workspace at the repository root, Rust 2024 edition in every Cargo.toml, one member crate 'atlas-core' as a stub (no schema yet: that is step 2), and a justfile whose 'check' recipe runs cargo fmt --all -- --check, cargo clippy --all-targets -- -D warnings, cargo test, and sw-checklist in that order. Add ingest/report/eval/snapshot recipes as stubs that exit non-zero with a message naming the step that will implement them, so the README's build section is true rather than aspirational. Audit .gitignore against what the workspace actually produces. Exit: 'just check' green with sw-checklist at zero failures and zero warnings, README build section accurate, and the commit pushed.