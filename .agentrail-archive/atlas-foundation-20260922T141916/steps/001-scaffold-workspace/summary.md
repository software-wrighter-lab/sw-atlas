Cargo workspace (Rust 2024 via workspace.package, unsafe_code denied, missing_docs warned), crates/atlas-core with SCHEMA_VERSION and PRODUCER plus tests over both, and a justfile carrying the gate (fmt, clippy -D warnings, test, sw-checklist) and four pipeline recipes that exit non-zero naming the step that lands them. just check green: 2 tests pass, sw-checklist 6 passed 0 failed 0 warnings. README build section rewritten to match what actually runs. Cargo.lock committed, /target/ already ignored.