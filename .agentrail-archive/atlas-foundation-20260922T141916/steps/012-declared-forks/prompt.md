Owner decision, 2026-09-22: include a GitHub fork in the repository corpus when the owner has written about it -- when a blog post or a campus place declares a link to it. Forks nobody declared stay excluded. Change 'atlas-ingest repos' to take the declared repository ids from the blog and campus corpora (the same link_id both already use) and keep a fork only if it is among them; record in the resource that it is a fork (an alias or summary note is not enough -- choose a field that does not widen the schema, or state why a schema change is justified). Update cache/README.md, README counts, the pinned count test, and the justfile recipe. Expected today: nine forks the blog names, among them sw-game-dev/game-mcp-poc, sw-embed/sw-cor24-pascal and softwarewrighter/bdh. Gate: just check green.