just report writes docs/reference/coverage.md from the corpus and gates on two faults: a resource nothing can reach (0) and a URL that is gone (0). Both demonstrated failing on seeded faults and passing on the real corpus, plus tests that a blocked host, a timeout, an unchecked URL and an unused concept are reported and NOT gated. 643 artifacts: 410 tagged, 470 related, 0 unreachable. All 642 distinct URLs checked: 635 ok, 0 gone, 6 blocked (ACM/doi/openreview), 1 no answer -- all 7 are cited outside works, named in the report. Results committed in cache/url-status.json so the offline run is deterministic; just report-check is the network path. New crates: atlas-links (HTTP only; split out because atlas-coverage hit the 4-module cap) and atlas-coverage with the atlas-report binary carrying build facts. sw-checklist 78/0/0, 57 tests.