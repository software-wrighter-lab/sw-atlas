Two repositories were cloned for inspection: ~/github/softwarewrighter/shorts and ~/github/softwarewrighter/video-publishing. Record what they do and do not give this project, because the answer changes step 009 (ingest-metadata) and open decision 3. The finding: shorts is a real text corpus deliberately kept in git (its .gitignore excludes media and keeps scripts, titles and descriptions), with 78 projects, 39 carrying narration, 63 carrying a published description, and a hand-built concept-to-episode index in docs/5mlc-concepts.md and docs/concepts-status.txt covering 130 concepts across 26 episodes. Thirty of the blog's 75 published videos are Five ML Concepts episodes, and episodes 1 to 29 join deterministically to projects/5MLC-N, so 29 videos get their full script with no human judgement. The rest need a hand-written map because titles changed between production and publication: projects/trm opens 'Under 1000 Parameters Beats GPT-4 at Mazes' while the blog's video_title for it is '976 parameters is more than billions?!'. video-publishing is not a video index: video-tracker.csv is a 111-row backlog with zero URLs and no publish dates, and for-daily-release.org is a schedule of intentions. Neither repository records a YouTube URL or video id per project, so the blog stays the authority for which videos exist and where they are, and shorts becomes the authority for what was said in them. Write docs/video-sources.md with those numbers and the join rule, note that the gitignored media and generated drafts are not needed for indexing, update plan.md section 12.3, and update step 009's prompt so the ingester expects a committed join map rather than inventing one. Exit: the survey recorded with its counts, just check green.