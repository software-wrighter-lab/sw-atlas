Surveyed shorts and video-publishing. shorts is a genuine text corpus kept in git on purpose (gitignore excludes media, keeps scripts/titles/descriptions): 78 projects, 39 narrations, 63 descriptions, 1350 tracked .txt, plus a hand-built index of 130 concepts across 26 episodes in docs/concepts-status.txt. 29 of the blog's 75 videos join deterministically (episode N to projects/5MLC-N, episode 1 excepted); the rest need a committed ~46-line map because titles changed between production and publication (projects/trm vs '976 parameters is more than billions?!'). video-publishing is a backlog, not an index: 111 rows, zero URLs, no publish dates. Neither repo records a per-project YouTube URL, so the blog stays the authority for what is published. The gitignored media needs no recovery from the old machine. Recorded in docs/video-sources.md; plan 12.3 and the ingest-metadata prompt updated. just check green.