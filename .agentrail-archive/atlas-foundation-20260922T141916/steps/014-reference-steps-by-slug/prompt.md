Three inserted steps in one session shifted the later step numbers three times, and each shift left stale 'step NNN' references in justfile, README.md and docs/. Stop referencing steps by number in documentation and recipe messages: name the step slug instead (concept-graph, coverage-report), which insertion does not change. Fix every stale reference now and leave no numbers behind except where a number is historical (a completed step in docs/sagas.md, a commit message). Gate: just check green.