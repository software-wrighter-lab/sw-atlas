demo-decision-model answered both sw-atlas requests on 2026-09-22, relayed by the owner. Request 2 is delivered: annotated tag tdm-v0.1.0 (commit b476842) pins crates/tdm-model and crates/tdm-trace, with docs/reference/crate-contract.md stating what a consumer may rely on -- bundle versions 2 and 3, the version field stable within a tag, parity tolerance 1e-9 measured on the rounded weights a consumer actually receives, serde and serde_json the only dependencies, edition 2024, MSRV 1.85, verified by a consumer crate built outside that repository. Request 1 (PR05 dynamic choice sets) is theirs: they are mid-build and sw-atlas must NOT write its own scorer. Record all of this in docs/demo-decision-model-requests.md: mark request 2 answered with the tag, the commit and the exact Cargo dependency lines to use; change request 1 from 'if sw-atlas gets there first it writes its own' to 'upstream owns it, Saga 3 step 3 waits on it'; note that they are adding a request file on their side so the ask survives without a relay; and record the compatibility note that a version 3 bundle may carry empty keywords and match_order when the labels are another program's rules. Add the dependency to docs/sagas.md so a later session sees that Saga 3 step 3 has an upstream owner. Also note in docs/hybrid-docent.md section 3 that the pin is now a tag rather than a revision to be chosen. Documents only, no code, no dependency added yet -- sw-atlas has no crate that consumes it until Saga 3. Gate: every relative link resolves, no trailing whitespace.